<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %></script>
<% end %>

<script>
  window.defaultTrackImage = "<%= asset_path('default-track-image.png') %>"
</script>

<main id="root">No reaction here!</main>